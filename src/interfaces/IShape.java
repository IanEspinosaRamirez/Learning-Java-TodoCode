package interfaces;
/* las interfaces dicen que es el objeto pero no pueden tener metodos para la instancia del mismo asi mismo deben tener al menos un metodo abstracto
* dan a entender que se debe hacer pero no el como*/
public interface IShape {
  public double calculateArea();
}
